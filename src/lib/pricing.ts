/** 課金プランの定義。
 *
 * 方針(重要): **無料プランの出力に透かしは入れない**。
 * 無料でも実用に足る透過 PNG をそのまま配布し、課金は
 * 「高解像度・商用利用の許諾・一括書き出し」で取る。
 */

/** 買い切り価格(税込・日本円)。Stripe には最小単位=円で渡す。 */
export const PRICE_JPY = 500;

/** Stripe の明細に出る商品名。 */
export const PRODUCT_NAME = "会社印ジェネレーター 高解像度パック(商用利用可)";

/** 無料プランで書き出せる PNG の一辺(px)。画面・PDF 貼付には十分なサイズ。 */
export const FREE_SIZE = 560;

/** 有料プランで一括書き出しする PNG の一辺(px)。2000px は印刷用途を想定。 */
export const PAID_SIZES = [300, 600, 1200, 2000] as const;

/** 有料プランの訴求文。UI と特商法表記の両方から参照する。 */
export const PAID_FEATURES = [
  "高解像度 PNG(最大 2000px)— 印刷・押印画像として使えるサイズ",
  "4 サイズ(300 / 600 / 1200 / 2000px)を一括ダウンロード",
  "角印・丸印の両方を同時に書き出し",
  "商用利用の許諾(請求書・見積書・契約書などの自社業務で使用可)",
  "購入証明つき利用許諾書(発行番号・購入日を明記)をテキストで発行",
] as const;
